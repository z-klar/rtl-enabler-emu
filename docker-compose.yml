version: '3'

networks:
  backend:

services:

  rtl-enabler-emu:
    image: "rtl-enabler-emu:latest"
    container_name: "rtl-enabler-emu"
    ports:
      - "9999:9999"
    networks:
      - backend

